import { TextInput, View } from 'react-native';

type Props = {};

export default function StartGameScreen({}: Props) {
	return (
		<View>
			<TextInput />
			{/* CUSTOM BUTTONS HERE */}
		</View>
	);
}
